<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:name=".billing.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OneStepToday"
        tools:targetApi="31">

        <activity
            android:name=".welcome.Welcome1Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".welcome.Welcome2Activity"
            android:exported="false" />

        <activity
            android:name=".welcome.Welcome3Activity"
            android:exported="false" />

        <activity
            android:name=".welcome.Welcome4Activity"
            android:exported="false" />

        <activity
            android:name=".fragments.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".fragments.CreateStepActivity"
            android:exported="false" />

        <activity
            android:name=".paywall.PaywallActivity"
            android:exported="false" />

        <receiver
            android:name=".fragments.ReminderReceiver"
            android:exported="false" />
    </application>

</manifest>
